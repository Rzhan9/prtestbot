# GitHub PR Test Coverage Review Agent

A GitHub Action and Python tool that automatically reviews Pull Request (PR) diffs to determine if the changes have adequate test coverage. 

It analyzes the code diff, locates related existing tests, identifies changed/added behavior, determines coverage status, and suggests concrete tests to write. It posts a formatted report as a PR comment and automatically updates its previous comment on subsequent commits to avoid spamming the PR conversation.

## Features

- **Behavior-Focused:** Does not review code style, performance, or unrelated issues. Focuses purely on: *"Did this PR add or update enough tests for the behavior it changed?"*
- **Smart Test Finding:** Locates existing test files using structural matching and naming convention similarity heuristics.
- **De-duplication:** Automatically finds and updates its previous review comments instead of flooding the PR with new comment threads.
- **Multi-Provider Support:** Supports Anthropic (Claude), Google Gemini, and OpenAI out of the box via lightweight direct HTTP APIs.
- **Local Dry-Runs:** Run the analysis locally against any repository diff without requiring a GitHub Action environment.

---

## Directory Structure

```text
test-coverage-agent/
  .github/
    workflows/
      test-coverage-agent.yml
  src/
    test_coverage_agent/
      __init__.py
      main.py
      github_client.py
      repo_analyzer.py
      diff_parser.py
      test_finder.py
      llm_provider.py
      prompt_builder.py
      report_formatter.py
  tests/
    test_diff_parser.py
    test_test_finder.py
    test_report_formatter.py
  README.md
  requirements.txt
  .env.example
```

---

## GitHub Setup (Actions)

To use the agent in any repository, add a workflow file at `.github/workflows/test-coverage.yml` with the following content:

```yaml
name: PR Test Coverage Review

on:
  pull_request:
    types: [opened, reopened, synchronize, ready_for_review]

jobs:
  review:
    runs-on: ubuntu-latest
    if: github.event.pull_request.draft == false
    permissions:
      contents: read
      pull-requests: write
      issues: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: Rzhan9/prtestbot@main
        with:
          llm_api_key: ${{ secrets.YOUR_API_KEY_SECRET }}
          llm_provider: gemini  # or: anthropic, openai
```

Then add your LLM API key to the repository's **Settings → Secrets and variables → Actions**:

| Provider | Secret name | `llm_provider` value |
|---|---|---|
| Google Gemini | `GEMINI_API_KEY` | `gemini` |
| Anthropic Claude | `ANTHROPIC_API_KEY` | `anthropic` |
| OpenAI GPT | `OPENAI_API_KEY` | `openai` |

That's all. No Python files, no pip installs — the action handles everything internally.

---


## Local Development & Testing

You can run the agent locally to inspect its prompt or review a local diff before deploying.

### Setup

1. Clone or navigate to the project directory:
   ```bash
   cd test-coverage-agent
   ```

2. Create a virtual environment and install dependencies:
   ```bash
   python3 -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt
   ```

3. Setup your environment variables by copying `.env.example` to `.env`:
   ```bash
   cp .env.example .env
   ```
   Open `.env` and fill in your LLM API Key (e.g. `GEMINI_API_KEY`, `ANTHROPIC_API_KEY` or `OPENAI_API_KEY`).

### Run a Local Test

To perform a review, you need a unified diff of the changes. You can generate one from git:

1. Create a diff file of your recent changes:
   ```bash
   git diff main > local_changes.diff
   ```

2. Run the main orchestrator script in local mode:
   ```bash
   export LOCAL_DIFF_PATH=local_changes.diff
   export PR_TITLE="Fix calculator divide by zero error"
   export PR_BODY="Adds check to divide function and throws ValueError when denominator is 0."
   
   # Run
   PYTHONPATH=src python src/test_coverage_agent/main.py
   ```

---

## Example Output Report

Below is an example of the markdown review comment generated by the agent on a pull request:

# Test Coverage Review Agent

## Verdict

Partially sufficient

## Summary of Changed Behavior

* Added error handling to `Calculator.divide` in `src/calculator.py` to prevent division by zero.
* Raised a custom `ValueError("Cannot divide by zero")` exception.

## Existing Relevant Tests Found

* `tests/test_calculator.py`

## Missing or Partial Test Obligations

### 1. Division By Zero Error Case

Changed behavior:
Raising ValueError in `Calculator.divide` when the second argument is `0`.

Coverage status:
Missing

Suggested file:
`tests/test_calculator.py`

Suggested test name:
`test_divide_by_zero_raises_value_error`

Suggested test scenario:
Arrange:
* Instantiate `Calculator`.

Act:
* Call `calculator.divide(10, 0)` inside a `pytest.raises(ValueError)` context block.

Assert:
* Verify that the caught exception message matches "Cannot divide by zero".

Optional test code:
```python
import pytest
from src.calculator import Calculator

def test_divide_by_zero_raises_value_error():
    calc = Calculator()
    with pytest.raises(ValueError, match="Cannot divide by zero"):
        calc.divide(10, 0)
```

## Notes / Uncertainty

None. Checked both `src/calculator.py` and `tests/test_calculator.py` successfully.

---

## Running Unit Tests

Run unit tests locally using:
```bash
pytest -v tests
```
